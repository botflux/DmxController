<UserControl x:Class="DmxController.Views.NetworkFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DmxController.Views"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="{DynamicResource MaterialDesignPaper}"
            TextElement.FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel Margin="16">
        <Label Content="Réseaux"/>

        <Label Content="Nom d'hôte" FontSize="10" Margin="0 16 0 0" />
        <TextBox Margin="0 8 0 0" />

        <Label Content="Port d'envoie" FontSize="10" Margin="0 16 0 0" />
        <TextBox Margin="0 8 0 0" />

        <Label Content="Port de reception" FontSize="10" Margin="0 16 0 0" />
        <TextBox Margin="0 8 0 0" />
    </StackPanel>
</UserControl>
